= simple_form_for @ad do |f|
	= f.input :variations
	= f.input :brief
	-if @new_ad


		= f.check_box :make_copy, {:checked=>true}
		I would like new copy created for this ad
		%br
		= f.check_box :make_image, {:checked=>true}
		I would like a new image created for this ad
		%br
		= f.button :submit, "Submit", class: "button"

